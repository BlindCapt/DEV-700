import { create } from 'zustand';

interface StoreState {
  // État initial vide, à compléter selon les besoins
}

export const useStore = create<StoreState>()((set) => ({
  // Store vide pour l'instant
})); 